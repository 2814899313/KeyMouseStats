# 主题资源扩展约定（1.3.0）

1. 在 `ArtTheme.All` 添加主题配色，在 `ThemeAssets` 中登记该主题的全部嵌入资源名称；图标放在列表前部以优先解码。
2. 在 build.bat / test.bat 与 README 手动编译命令登记资源映射。资源必须位于项目 assets 内，不能引用生成器缓存目录。
3. 推荐用图集：小丑牌为 2×3 背景与 4×4 图标；碧血丹心为 2×3 背景、6×4 UI 物件和一张小组件专属背景。绘制时传入整数裁剪区域，不同区域会获得不同缩放缓存标识。
4. 原图只经 `NikkiArt.Load` / `ThemeImages.Get` 获取。不要在绘制类中长期保存 Image 引用；当前主题生命周期结束时这些引用会被释放。
5. `ThemeImages` 的公开获取、激活与 `NikkiArt.SyncTheme` 只在 UI 线程调用。后台线程只串行解码，以代次检查后发布；不直接修改正在绘制的图像。
6. 单套主题的解码原图预算为 96 MiB，以宽 × 高 × 4 计算；超预算资源不加入原图池，界面保留底色。临时编解码缓冲不在此预算内。不要靠无限提高上限容纳巨大图片。
7. 当前页面仅保留一个大尺寸背景缓冲，页面 / DPI / 尺寸改变时替换；常用图标在独立小缓存中复用。原图在当前主题内始终保留，重建背景不需要重新 PNG 解码。
8. 切换主题调用 `NikkiArt.SyncTheme()`，释放旧原图和预缩放表面。不要为了“永不驱逐”常驻所有主题。
9. 验证 ThemeLoadingTests、ThemeOwnershipTests：页面复访不增加 DecodeCount，旧请求不能污染新主题，图标压力不触发背景重建，关闭美术主题后内存引用归零。
