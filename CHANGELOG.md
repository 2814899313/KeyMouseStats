# 1.7.3

## 按键时长

- 新增「按键时长」报告页：8 档按住时长直方图、每键平均时长排行、覆盖率与丢弃样本数。
- 时长取首次按下到抬起的间隔，由单调时钟测量；系统的自动重复不会重置起点。
- 超过 60 秒的按住、丢失抬起、锁屏与退出时未抬起的按键一律丢弃并计入丢弃数。
- 页面明确标注两件事：按住时长不是按压力度（普通键盘没有压力感应）；丢抬起是常态，覆盖率偏低时结论要谨慎。

## 应用 × 键位

- 新增「应用键位」报告页：应用行 × 六类键位分组的构成热图与完整明细。
- 每次击键把当时的前台进程与键位分组一起累加；每天只保存击键最多的 12 个应用与「其他应用汇总」，避免逐键 × 应用的矩阵撑爆文本格式。
- 未归因的击键单列，不按比例分摊；页面只描述按键构成，不推断应用用途。

## 数据保留与归档

- 保留期可配置（30—3650 天，或永久保留），入口在数据管理对话框。
- 超期数据不再直接删除：先按月度折进 `archive_v1` 聚合（合计、活跃时长、应用 Top 16），再移除明细；归档不参与任何日级统计。
- 修复了此前保留期硬编码 365 天、超期数据无声消失的问题。

## 数据格式 v11

- 新增可选字段 `hold_v1`、`hold_key_v1`、`app_keys_v1`、`archive_v1`、`keep_days`，并写入 `format` 版本标记。
- v10 旧文件照常读取，缺少的维度显示「从 1.7.3 起采集」，不反推、不补零。
- 文件由更新版本写入时，数据管理对话框会给出提示。

## 工程

- 版本号单一来源的收益兑现：本次发版只改了 `Version.cs` 一处。
- 新增 60 项按键时长 / 应用键位 / 归档回归检查，报告页检查从 58 项增加到 75 项。

# 1.7.2

## 每日目标

- 可设置每日目标：按击键数或按活跃时长，随时可关闭；达成当天只提示一次，不反复打扰。
- 小组件单击在 今日 / 目标 / 累计 三种模式间循环，窗口尺寸不变；目标模式显示进度环、已用 / 目标与剩余量。
- 区间回顾报告新增「目标达成天数」与「连续达成天数」两行；缺失日跳过，不打断连续计数。

## 提醒

- 连续使用提醒：默认关闭，阈值 15–480 分钟、冷却 5–240 分钟可调。
- 免打扰：前台窗口全屏（游戏 / 全屏视频）时自动抑制提醒。
- 每日摘要：到设定时间后当天提示一次今日概况。
- 提醒基于「连续活跃段」，与空闲阈值是两个独立设置；修改提醒不会改变任何统计口径。
- 新增「目标 / 提醒」设置对话框与托盘入口。

## 工程

- 版本号改为单一来源 `Version.cs`：程序集属性、界面文字与 CI 校验都读它，发版只需改一处。
- 剩余 6 个测试改用 `Store.DataDirectory` 重定向数据目录，不再用反射改写私有字段。
- 新增 41 项目标与提醒回归检查。

## 其他

- 产品和程序集版本更新为 1.7.2；数据格式仍为 v10（新增可选 `wellbeing_v1` 设置），不迁移或改写历史统计。

# 1.7.1

## 数据管理

- 每天首次启动自动留一份带日期的备份，并按保留份数（默认 7，可在界面调整）清理旧备份；数据管理对话框可立即备份或打开数据目录。
- 导出为带 SHA-256 校验和的单文件；导入前先校验，校验不通过不会写入任何数据。
- 多机合并提供两种策略，并在写入前显示冲突预览：**保留击键更多的一份**（不会重复计数，两台机器同时使用时可能低估）或**两份相加**（分开使用时正确，同时使用时可能重复计数）。
- 逐日数据校验：活跃秒、空闲秒、小时分桶、逐键计数、段时长、应用时长与交叉观测等不变量，产出错误 / 警告清单。
- 合并后「累计总数」为近似值，界面明确标注；日记录本身按策略精确合并。

## 呈现交互

- 区间回顾图表支持拖拽刷选子区间，松开后直接切换为自定义区间。
- 区间回顾新增对比区间选择：紧随其前 / 去年同期 / 自定义。
- 报告头部「导出」改为下拉菜单：导出 CSV / 导出图片 PNG / 导出网页 HTML。
- 网页导出为自包含单文件：图表以 base64 内嵌、明细为表格、不含脚本、离线可开。

## 其他

- 产品和程序集版本更新为 1.7.1；数据格式仍为 v10（新增可选 `backup_keep` 设置），不迁移或改写历史统计。

# 1.7.0

## 区间分析处理层

- 新增 `RangeAnalysis.cs`：区间口径、聚合、分布、趋势与对比只在这一处定义，页面不再各算一套。
- 口径统一为：终点不超过最近一个已结束的日、缺失日不补零、日均按有效天数计算、基准为 0 或样本不足时显示「基准不足」。
- 新增分布统计（P10/P50/P90、标准差、变异系数、直方图分箱，分箱计数恒等于样本数）与趋势拟合（按实际日期间隔的线性斜率、拟合优度、按星期去均值的周期偏移）。
- `PersonalStats` 的均值、分位数、标准差改为委托同一实现；`WeekAnalysis` 的日期口径与星期索引同理。
- 新增 60 项区间分析回归检查（边界、缺失、空区间、分箱守恒、趋势斜率、星期偏移、对比）。

## 报告页

- 「本周回顾」升级为「区间回顾」：报告顶部新增区间选择器（近 7 天 / 近 30 天 / 本月 / 上月 / 本季度 / 今年 / 自定义），对比紧随其前的等长区间；同时给出合计变化与日均变化，避免有效天数不同时误读。
- 新增「分布」页：三行箱线图（逐日击键、连续段时长、单应用单日时长）与逐日击键直方图；样本少时自动减少分箱，样本完全相同时只画一个标记。
- 报告导航重排为四组：时间与节奏 / 应用使用 / 输入方式 / 周期与分布。
- 新增 48 项区间回顾、分布与星期节律回归检查。

## 其他

- 产品和程序集版本更新为 1.7.0；数据格式仍为 v10，不迁移或改写历史统计。

# 1.6.0

## 下载体积

- 34 个不含透明像素的主题资源（背景、立绘、组件）改为 JPEG q90 嵌入，7 个含透明像素的图标图集与 UI 物件保持 PNG。
- `KeyMouseStats.exe` 从 82.0 MiB 降到 22.6 MiB，约为原来的 1/3.6；仍是单文件、双击即用，不改变分发方式。
- 逐张记录转换误差：平均像素差 1.4/255、最大 3.0/255；201 张渲染对比图平均差 0.17/255，逐主题 100% / 150% 检查未见可见差异。
- 原 PNG 母版保留在 Git 历史中，需要重新编码时用 `git show <提交>:assets/<名称>.png` 取回；编码约定见 THEME_RESOURCES.md。

## 持续集成

- `test.bat` 新增 `--logic-only`：跳过依赖交互式桌面会话的 RenderDashboard 与 ThemePerf，其余 19 个测试照常运行。
- 构建与发布工作流都会先运行这 19 个测试；测试失败时不再产出产物，也不会发布版本。
- 期望版本改为从 `AssemblyInfo.cs` 读取，发版不再需要手改工作流里的版本号。

## 其他

- 产品和程序集版本更新为 1.6.0；数据格式仍为 v10，不迁移或改写历史统计。

# 1.5.0

## 存储可靠性

- 悬浮小组件的保存间隔从 3 秒延长到 30 秒，磁盘写入量约降到十分之一；崩溃最多丢失 30 秒计数，与原先每次落盘的计数口径一致。
- 序列化仍在 UI 线程完成，文件写入移到后台线程，避免保存阻塞低级钩子的输入处理；退出程序与校准 DPI 后仍会等待真正落盘。
- 每次保存带递增序号：后台旧快照不会覆盖之后完成的同步保存，连续保存只落盘最新快照。
- 数据目录改为可注入，测试不再用反射改写私有字段；新增 22 项存储可靠性检查（原子替换备份、异步与同步混用顺序、合并写入、保留窗口）。

## 本周回顾

- 统计报告新增「本周回顾」页：最近 7 个完整日对比再往前的 7 日，覆盖击键、日均击键、鼠标点击、滚轮事件、组合动作、活跃时长、日均活跃、连续使用段、平均段长与移动距离。
- 两期分别显示有效天数（例如「本期 6/7 天 · 上期 7/7 天」）；今日尚未结束，不计入本期；缺失日不补零，日均按有效天数计算。
- 柱状图按同一星期几并排对照两期，鼠标悬停查看两期数值。
- 「使用洞察」页新增「本周回顾」入口，一键打开该页。

## 星期节律

- 统计报告新增「星期节律」页：按星期聚合最近 8 周（56 天）的已结束日，展示样本天数、日均击键、日均活跃、日均点击与最忙时段。
- 图表每个星期一根日均击键柱、一根日均活跃柱；样本天数很少时结果容易被单天影响，页面明确标注。

## 其他

- 统计报告新增「导出图片」：把当前页图表按 2 倍尺寸导出为 PNG，便于分享。
- 修复 RenderDashboard 渲染测试的等待顺序错误：`WaitTheme` 在切换主题之前调用，而主题资源只加载当前主题，导致该测试自 1.3.0 起一直失败。预览图片仍会生成，因此此前未被发现。
- 产品和程序集版本更新为 1.5.0；数据格式仍为 v10，不迁移或改写历史统计。

# 1.4.0

- 新增“欧洲卡车 · 长途之路”完整美术主题，六个数据页面使用独立公路场景。
- 悬浮组件启用独立驾驶舱背景，图标、按钮、卡片与加载动画统一为夜航仪表风格。
- 页面背景采用等比覆盖裁切，避免宽高比变化导致车辆与场景拉伸。
- 主题资源继续按当前主题独占加载，约 50 MiB 解码占用保持在 96 MiB 预算内。

# 1.3.2

## 多日小时曲线与默认窗口行为

- “近 N 天”改为每天一条独立小时曲线，包含所选日，支持 2–14 天；新增“本周 7 天”多曲线。
- 所选日采用主题主色粗线，其他日期使用独立颜色虚线；图例保持紧凑，悬停浮层显示最多七天的逐日数值。
- 图上不再绘制历史均值线。底部差值摘要仍以其余可见日期的同期均值为基准，并明确标注。
- 小时 CSV 改为宽表结构，每个显示日期对应一列。
- 桌面悬浮小组件的窗口置顶默认关闭，托盘菜单可随时开启。游戏 HUD 的置顶行为不变。
- 产品和程序集版本更新为 1.3.2。
- 同版本新增 KeyMouseStats.Clean.exe：保留全部基础功能，强制轻量默认界面，不嵌入皮肤图片，也不显示主题选择器。新增 build-clean.bat 与双版本 GitHub Actions 构建。
# 1.3.1

## 小时趋势对比升级

- 对比方式统一收纳为一个入口，支持关闭、昨天、上周同日和自定义近 2–90 天均值。均值按每个小时的有效样本计算，缺失日期不补零。
- 修复所选日峰值与对比日纵轴最大值共用变量，导致“最高小时量”和主题峰值贴纸可能错位的问题。
- 悬停信息新增绝对差与百分比；红蓝色带呈现逐小时高低差；底部摘要按双方重叠的已记录小时展示对比值、相差数量和相差比例。
- 当前小时之后的未来区域增加遮罩；小时 CSV 新增对比方式和差值列。
- 产品和程序集版本更新为 1.3.1。
# 1.3.0

## 大侠立志传 · 碧血丹心大型美术更新

- 新增第十一套美术主题。六页使用六位可结缘女角色的独立精绘场景：洛千雪、曲忘忧、韩红玉、绛紫烟、顾倾城、苗彩蝶。
- 新增专为桌面悬浮小组件绘制的独立背景，按小窗口信息结构预留标题、四项指标和 APM 微趋势区域。
- 新增 24 枚武侠 UI 物件，覆盖导航、统计指标、峰值徽章、载入状态与键帽纹样。
- 键盘热力按六类键位分别绘制水墨山、方孔钱、竹简、朱砂官印、罗盘和古琴纹样；最高频键使用盟主金边。
- 按钮、卡片、状态徽标、页面题签和加载动画全部采用主题专属绘制，不再局限于背景与配色替换。
- 三张资源继续由 ThemeAssets 按当前主题持有，切换主题即释放；没有扩大 96 MiB 原图预算和 40 MiB 缩放缓存上限。
- 产品和程序集版本更新为 1.3.0。

验证：六页主题、六类键帽、小组件背景与资源预算检查通过；六套美术主题 100%/150% 共 72 次切页通过。碧血丹心主题原图常驻约 18 MiB，最慢暖帧约 10.9 ms（100%）/ 18.3 ms（150%），为本机离屏结果。

- 修复碧血丹心六页背景图集与桌面小组件母版比例不匹配造成的拉伸；素材分别重排为方形 2×3 图集和 6:5 小组件画幅。
- 背景绘制改为等比例 cover 裁切，今后资源尺寸变化也不会产生非等比缩放。

# 1.2.2

## 小时趋势对比

- 「趋势 → 按小时」新增“对比昨天”和“对比上周同日”，以所选日期为基准分别回溯 1 天或 7 天。
- 所选日使用主题主色实线，对比日使用橙色虚线，两条曲线共用纵轴，避免比例不同造成误读。
- 悬停小时同时显示两个日期的数值；缺失历史保持“无记录”，今日未来小时继续留空。
- 小时趋势 CSV 增加对比日期和对比值列；产品和程序集版本更新为 1.2.2。
- 修复小时页“按天 / 按小时”与对比按钮命中区域重叠，导致“对比昨天”无法选择的问题。

# 1.2.1

## 小丑牌 · 幻彩牌桌

- 新增第十套主题：暗青牌桌、珊瑚红 / 青蓝点缀、纸牌小丑、扑克筹码与像素道具图标。
- 使用内置 imagegen 生成六场景背景图集与 16 枚透明图标图集；六个页面分别取用不同场景，键盘最高频键显示小丑徽章。
- 配套蓝至红的主题热力色带、牌桌载入提示与报告页徽章，素材继续内嵌 EXE。
- 色卡调整为四列，新增主题不会与下方快捷操作区重叠。

## 主题资源管理

- 新增 ThemeAssets 注册表，按主题声明资源。激活主题后后台串行预加载，图标优先。
- 当前主题原图保持常驻，翻页不再参与全局 LRU 淘汰；切换主题时释放旧主题资源，避免内存随安装主题数量增长。
- 完成队列统一发布；主题代次隔离在途解码，快速换主题不会回填过期结果。缺失资源在本次激活中不重复请求。
- 解码后的原图预留与常驻合计预算 96 MiB，超预算资源保留底色兜底；该预算不包含编解码临时内存、窗口与统计数据。
- 当前页面背景使用独立预缩放缓冲（最多 36 MiB），图标使用独立 4 MiB / 128 项缓存；图标压力不会驱逐当前背景。换页或尺寸变化时只重建当前背景缓冲，不保留所有尺寸的副本。
- 缓存标识包含原图身份、裁剪区域、尺寸和透明度，防止不同图集区域串图。
- 原图分辨率不降低；超出缩放缓存预算时直接绘制，不持续分配巨大缓存。

验证：五套美术主题六页、100%/150% 缩放共 60 次切页；167 项资源所有权、快速换主题、重复解码、图标压力与图集身份检查。以上为本机离屏验证，不代表所有设备的帧率。

# 1.2.0

## 会话 × 应用交叉归因

- 在已有真实会话归因数据上增加跨会话一致的应用颜色：全日 Top 4 应用、其他应用、未识别、未采集分别展示。
- 最近 6 段用堆叠条呈现，悬停查看进程路径、时长和占比；明细 / CSV 包含全部会话、应用秒数、整段占比及开始原因。
- 应用归因覆盖以会话有效时长合计为分母。旧记录不按全天应用占比反推；未识别与未采集分别保留。

## 快捷键节省量（情景估算）

- 统计报告新增「快捷键节省」，展示匹配次数、预计少用的鼠标点击、预计省时、模型覆盖率与贡献排行。
- 首批模型匹配 Ctrl+C/V/X/Z/S/A/F/P；只有假设其用于对应菜单命令时估算才有意义，程序不验证命令执行成功。
- 默认假设菜单 2 秒 / 2 次点击、快捷键 0.5 秒；每次省时 = max(0, 菜单秒数 − 快捷键秒数)。均为可调整的场景参数，不是实测或人群基准。
- 顶部「模型」可调整参数并重新计算，参数随设置保存。未知组合保留在明细，不赋予节省量；不扣减实际活跃时间。
- 产品和程序集版本更新为 1.2.0；沿用 v10，增加可选 shortcut_model_v1 设置。

# 1.1.2

- 按键排行新增「近7天」「近30天」，保留今日与近90天，均包含今天。
- 单键、组合键、键盘热力和右侧概况使用同一日期范围；组合键及热力导出同步筛选。
- 修复按键概况的键鼠比一直使用全部累计数据的问题，改为所选范围。

# 1.1.1 修复版

## 连续使用段计时

- 修复 UI 采样超过 5 秒就无条件断段的问题：仅当上一条输入的空闲阈值仍覆盖整个延迟区间、当前仍活跃且两个时钟一致时，保留同一段并计入这段确定的活跃时间。
- 不能确定活跃连续性的采样间隔继续排除；锁屏、休眠、断连、重启和修改阈值仍然断段，跨日分别计入两天。
- 墙上时钟与单调时钟在前台应用识别之前配对采样，避免应用识别延迟被误判成系统时间跳变。延迟超过 5 秒的应用时间仍不按前台端点强行归因。
- 连续使用段列表新增「开始原因」，区分空闲后恢复、采样中断、解锁、休眠、启动、时钟异常等；原因随数据保存。
- 明确段末已包含阈值内缓冲，因此段间空白可以小于设定的空闲阈值。悬浮显示毫秒时间与精确时长，避免整秒显示产生约 1 秒差异的误解。
- 沿用 v10 格式并新增可选 `session_reason_v1` 元数据。旧记录缺少原因时标为未标注，不合并、不补算历史时长。
- 程序及产品版本更新为 1.1.1。

# 1.1.0 正式版

## 统计报告

- 十个报告页改为三组侧栏导航；窄窗口使用下拉选择，内容区支持滚动。
- 输入习惯重做为三项概览、个人基准说明和六张指标卡；明细默认折叠。无可用历史基准时隐藏比较列，不画误导性的比较条。
- 每项指标提供通俗名称、单位和悬浮解释；P50/P90 的含义和建立条件放在数据旁，详细口径通过顶部说明展开。
- 简化小数显示、加强明细行隔色。鼠标移动/操作以厘米呈现，采集和存储仍用米。
- 顶部导出 CSV 支持选择保存位置；选中指标卡或明细行后可复制。输出保留计算精度，不以显示舍入值替代原值。
- 明确区分今日未结束、数据快照时间与实时更新，窗口标题简化为「统计报告」。
- 统一九套主题配色、卡片层级和轻量渐变；未增加动画计时器与大型背景资源。
- 版本号更新为 1.1.0，沿用 v10 数据格式，升级不迁移或改写历史统计。

# 1.0.0 正式版

2026-09-04

## 跨维度统计

- **应用时段**：按实际前台活跃观测保存每个应用的小时分布，提供 Top 8 热图、覆盖率及全部时段明细。
- **应用操作**：按进程合并击键、点击和滚轮，展示应用内部构成与绝对事件数。
- **会话应用**：记录每个连续使用段内的应用活跃时间，显示主要三个应用、其他与未归因部分。
- 时间型交叉数据自 1.0.0 起采集。旧记录不按比例分摊，也不伪造关联。小时聚合按日最多 128 个进程，会话最多 32 个进程，超出后以“其他应用汇总”保存。

## 鼠标矢量

- 相对 Raw Input 保存左、右、上、下绝对轴分量。方向比例分母为 `|dx| + |dy|`，不是欧氏路程。
- 快速移动候选：约 50–80 ms 的向量窗口内路程至少 8 mm，冷却 200 ms。
- 掉头：同设备相邻向量各至少 2 mm，方向变化至少 120°，间隔至多 120 ms，冷却 150 ms。暂停、设备变化及 DPI 改变不拼接向量。
- 移动点击协同：统计任一按钮按住期间的移动比例、点击前 200 ms 内观察到移动的比例。它们不判定游戏命中率、拖动是否成功、左右手或真实意图。
- 快速移动和掉头需正确设置 DPI；方向分量无需 DPI。绝对坐标设备及距离校准过程不参与矢量分析，只保存聚合值，不保存逐次轨迹。

## 键位语义

- 默认游戏键位映射：WASD/方向键、数字/技能栏、空格/Shift、功能/修饰键、文字及其他、未归位记录。
- 各组互斥，已有逐键数据可直接分析；组合动作另计，避免重复计入分母。这是键位结构，而非游戏用途识别；例如文本中的 WASD 也按该映射统计。

## 异常标注

- 趋势图与日历以橙色轮廓标记历史击键高值，悬停显示较此前 7 日均值的变化。
- 判断使用候选日之前最多 30 天、至少 14 个有效观测日的均值与样本标准差，阈值为 +2σ；缺失日不补零、无波动基准不作判断，今日未结束不标异常。
- 7 日变化要求此前 7 天均有记录且均值非零。异常属于描述性提示，不是显著性或效率评价。

## 报告与分发

- 统计报告新增五页，全部支持主题配色、悬停详情、完整明细和按页 CSV 导出；新页为打开时快照，不增加持续刷新任务。
- EXE 产品版本为 **1.0.0**，文件及程序集版本为 **1.0.0.0**。所有主题资源仍内嵌，可分发单个 `KeyMouseStats.exe`。
- 数据格式升级为 v10，兼容读取旧数据；存储目录不变。重启后开始积累新增观测，保留原统计总数。

